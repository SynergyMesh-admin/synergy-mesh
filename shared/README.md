# 共享資源

此目錄用於存放跨版本共享的資源和工具。

## 目錄結構

```
shared/
├── configs/       # 共享配置
├── scripts/       # 共享腳本
├── types/         # 共享類型定義
└── utils/         # 共享工具函數
```

## 使用指南

1. 共享配置應放置於 `configs/` 目錄
2. 跨版本使用的腳本放置於 `scripts/` 目錄
3. TypeScript 類型定義放置於 `types/` 目錄
4. 通用工具函數放置於 `utils/` 目錄
